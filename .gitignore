# Build artifacts (compiled by build.ps1 / ps2exe)
*.exe

# Logs / transcripts
*.log
logs/

# Per-machine resume state + scratch (not part of the app)
.resume-session
resume.txt

# OS / editor cruft
Thumbs.db
desktop.ini
.vs/
*.user
